# FaceXpert-Guard
FaceXpert Guard - Built a PC facial recognition system from scratch using Siamese neural networks. Captures face images via OpenCV, trains a custom model with embedding layers and L1 distance, then verifies through a Kivy GUI.
